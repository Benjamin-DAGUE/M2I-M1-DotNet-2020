<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CoursWPF.FirstApp"
                    xmlns:models="clr-namespace:CoursWPF.FirstApp.Models">

  <DataTemplate x:Key="PersonTemplate" DataType="{x:Type models:Person}">
    <Border Margin="5" 
                    CornerRadius="10"
                    Background="CornflowerBlue"
                    HorizontalAlignment="Stretch">
      <StackPanel Orientation="Vertical">
        <TextBlock Text="{Binding FirstName}" Style="{StaticResource TextBlockStyle}"/>
        <TextBlock Text="{Binding LastName}"  Style="{StaticResource BoldTextBlockStyle}"/>
      </StackPanel>
    </Border>
  </DataTemplate>

</ResourceDictionary>